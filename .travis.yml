language: python
python:
  - "2.7"

notifications:
  email:
  - david.moser@bitmovin.net

install:
  - pip install -r requirements.txt

script:
  - coverage run --source=bitcodin setup.py test

after_success:
  - coveralls